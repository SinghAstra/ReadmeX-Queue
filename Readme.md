# ReadmeX-Queue

This project manages asynchronous jobs for ReadmeX, using a queue-based architecture with Redis and interacting with services like GitHub and Google Gemini AI. It provides APIs for adding jobs, cleaning up jobs, and monitoring progress via a Pusher server.
